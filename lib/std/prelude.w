// std.prelude — ambient imports for the self-hosted frontend.
//
// This file intentionally uses plain `use` declarations (no re-export
// syntax dependency) so it works with the current parser/import model.

use std.math
use std.string
use std.builtins
use std.collections
use std.option
use std.result
use std.iter
use std.fs
use std.process
