// std.str — compatibility import surface for string utilities.
//
// The canonical implementation lives in std.string. This module exists so
// user-facing code can import the shorter std.str name.

use std.string
