// lib/std/zlib/bundle.w — the zlib .wo bundle root (docs/wo_bundles.md).
// Written by build/zlib.w (zlib-migrate) from the migrated module list:
// one `use` per corpus module; example and minigzip are the harness.
use std.zlib.adler32
use std.zlib.compress
use std.zlib.crc32
use std.zlib.deflate
use std.zlib.defs
use std.zlib.gzclose
use std.zlib.gzlib
use std.zlib.gzread
use std.zlib.gzwrite
use std.zlib.infback
use std.zlib.inffast
use std.zlib.inflate
use std.zlib.inftrees
use std.zlib.trees
use std.zlib.uncompr
use std.zlib.zutil
