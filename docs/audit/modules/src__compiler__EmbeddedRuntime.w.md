# Audit: src/compiler/EmbeddedRuntime.w @ 450733e5

Commit: 450733e5 (`git rev-parse --short HEAD` => `450733e5`)
Module: `src/compiler/EmbeddedRuntime.w` (33 lines, read in full)
Generator: `build/runtime.w` (`br_generate_embedded_runtime`, lines 289-332;
  `br_collect_runtime_files`, lines 132-142) via `compiler.EmbeddedRuntimeData`
Pattern source: `src/compiler/EmbeddedStdlib.w` (37 lines, read in full)
Callers read: `src/Resolve.w` (import only), `src/compiler/ModuleSource.w:19-29`,
  `src/compiler/Zcu.w:326-332`, `src/compiler/Frontend.w:1420-1441`
  (`embedded_rt_resolve_path`), plus `src/compiler/Link.w:29-40,723-733`
  (embedded rt *objects*, separate mechanism, out of scope).
Stage1: `out/bootstrap/bin/with-stage1` exists (`ls` confirmed).

## What the module does

Thin facade over generated `compiler.EmbeddedRuntimeData` (D30 R2a):
repo-relative `rt/*.w` sources resolve under the synthetic `<embedded-rt>/`
prefix, keyed by rel path (`rt/rt_core.w`). Six `pub fn`s: `EMBEDDED_RT_PREFIX`,
`embedded_rt_is_module_rel`, `embedded_rt_source`, `embedded_rt_list_modules`,
`embedded_rt_display_path`, `embedded_rt_resolve_path`, `embedded_rt_rel_path`.

## Targets traced

- T13 (ownership/drop): no `Vec`/extern/manual-drop in this file. `str`
  returns are either static-data passthrough (`embedded_rt_source` @ line 12-13,
  `embedded_rt_list_modules` @ line 15-16) or a fresh `++` concat
  (`embedded_rt_display_path` @ line 18-19). Callers (`ModuleSource.w:28`,
  `Zcu.w:330`, `Frontend.w:1434-1440`) consume the text as borrowed parse input,
  exactly like the stdlib path. No owned/clone contract is broken here.
- T15 (migration fidelity vs `EmbeddedStdlib.w`): line-for-line mirror modulo
  three deltas, each refuted as a defect below (O1-O3).
- T22 (spec conformance, D30 R2a/R2b): prefix `<embedded-rt>/` distinct from
  `<embedded-std>/`; `is_module_rel` guard `starts_with("rt/")` matches the
  generator key space (`list_files("rt")` + `br_normalize_path_separators`,
  `build/runtime.w:137-141`); empty-source and non-`rt/` rels both map to `""`;
  `rel_path` slices exactly `prefix.len()`. R2b consultation confirmed at
  `Frontend.w:1434`, `ModuleSource.w:26-28`, `Zcu.w:328-330`.

## Findings

No defects surviving refutation. Numbered observations (all non-blocking):

1. `src/compiler/EmbeddedRuntime.w:21-28` (resolve lacks bundle-interface
   branch; cf. `EmbeddedStdlib.w:22-31`) — severity: none/info — target: T15 —
   probe: P1 (below) passes — refutation: `ModuleSource.module_source_read`
   (`ModuleSource.w:20-22`) checks `bundle_interface_text` BEFORE either
   embedded tree, so an rt path shadowed by a bundle still resolves to its
   `.wi`; the only direct `embedded_rt_resolve_path` caller bypassing bundles
   is the rt-in-unit join (`Frontend.w:1434`), whose members are never
   bundle-provided (bundles carry std-corpus `.wi` per D39/`BundleInterfaces`).
   Divergence is intentional, not a fidelity gap.
2. `src/compiler/EmbeddedRuntime.w:7-29` (`pub fn` throughout vs `fn` in
   `EmbeddedStdlib.w`) — severity: none/info — target: T15 — probe: n/a
   (compiles; P1 passes) — refutation: callers span module trees
   (`src/Resolve.w:13`, `src/compiler/ModuleSource.w:9`,
   `src/compiler/Zcu.w:13`, `src/compiler/Frontend.w:16`); wider visibility is
   required and satisfied. Not a defect.
3. `src/compiler/EmbeddedRuntime.w:15-16` (`embedded_rt_list_modules` has no
   in-repo caller; only def site; cf. std list called from `src/Lsp.w:1439`) —
   severity: none/info — target: T22 — probe: n/a — refutation: searched with
   REGEX `embedded_(std|rt)_(list_modules|resolve_path|rel_path)` over
   `src,build,docs`; pub API parity for future tooling, zero dead-code risk
   (one-line passthrough to generated data). Not a defect.
4. `src/compiler/EmbeddedRuntime.w:5` (`use compiler.EmbeddedRuntimeData`, no
   checked-in file) — severity: none/info — target: T22 — probe: P1 passes —
   refutation: by design the module is generated by
   `generate_compat_runtime_action` (`build/runtime.w:334-356`, requires
   non-empty `rt_files`, fails closed otherwise); stage1 binary embeds and
   serves it (P1). Not a defect.

## Probes run

- P1 (positive, end-to-end): `/tmp/rtprobe/main.w`
  (`pub fn main() -> i32:` + `print("hi-rt")`) via
  `out/bootstrap/bin/with-stage1 run main.w` => printed `hi-rt`, exit 0.
  Exercises the rt-in-unit join through `embedded_rt_resolve_path` +
  `parse_imported_file_frontend`. PASS.
- N1 (negative control): first probe draft omitted the `-> i32` return type;
  stage1 emitted `error: public function declarations require an explicit
  return type` + `error: run failed` with no crash/panic — resolve/error paths
  fail gracefully. PASS (graceful-error control).
- N2 (negative control, static): edge inputs traced by reading, not executed:
  `""` / `"rt"` (no slash) => `is_module_rel` false => `resolve` returns `""`;
  unknown `rt/*.w` => generated `embedded_rt_source_data` falls through to
  `return ""` (`build/runtime.w:329`) => `resolve` returns `""`; callers treat
  `""` as miss (`Frontend.w:1435-1437` emits `rt-in-unit` diagnostic and
  returns). No crash path.

Caller searches used REGEX mode only
(`embedded_rt_|EMBEDDED_RT_PREFIX|EmbeddedRuntime`,
`embedded_(std|rt)_(list_modules|resolve_path|rel_path)`).

## Verdict

Verdict: COMPLETE — no actionable defects; T13/T15/T22 all hold for this module at 450733e5.
