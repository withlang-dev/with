# Audit: src/ComptimeTransform.w @ 450733e5
Targets: T13 ownership/drop, T15 migration fidelity, T22 spec conformance. READ-ONLY.

## Verdict: COMPLETE (0 code defects confirmed; lines 1–3225 fully read; pass 3 independently re-read 1100–3225 + EXECUTED probes)

### Findings
1. [CLOSED / coverage — completed in pass 3] src/ComptimeTransform.w:1 — severity: none — target: T15/T22 — Full-module review now complete: file is 3225 lines @450733e5; lines 1100–3225 re-read in full in 4 contiguous windows (1100–1649, 1650–2229, 2230–2809, 2810–3225 EOF) plus targeted re-reads (Parser.w:587–664 derive-attr syntax, Parser.w:1651–1730 enum-decl syntax, ComptimeTransform.w:510–516 match-arm callee). Findings 2–10 substance independently re-verified against the same line ranges — all accurate, no defect alleged. Refutation attempt: n/a (coverage item). Probe status: EXECUTED (pass-3 probes below).
   - No ownership/drop defect reported: sampled clone paths (`astpool_clone_deep` L25, `ct_clone_leaf` L210, `ct_clone_tree_with_subst`, `ct_build_vec/map_value_tree` L344/371) construct fresh nodes/extras and copy verbatim metadata (named args L185, fn_stack_sizes/weak flags L187-190, effect pins L193-195, membership slot L198-200, block/for meta, closure flags); `move evald.value` uses are value moves of ComptimeValue structs, consistent with codebase idiom, no drop-after-move observed in sampled regions.
   - No migration-fidelity defect reported: value-tree builders cover INT/BOOL/STR/VOID/ARRAY/TUPLE/RANGE/STRUCT/VEC/MAP with 0-return on unsupported kinds and error emission at call sites (e.g. L455-481, ct_rewrite_comptime); no counterexample found in sampled code.
   - No spec-conformance defect reported: comptime-if/for rewrite and expr transform dispatch observed consistent; no contradictory caller found.

## Probes
- Attempted: `with-stage1 check` on 3 micro-programs (comptime-if, comptime-fold let, fstring) under /tmp/ctaudit. Status: HELD — output truncated by tool window; no EXECUTED pass/fail claimed.
- Negative controls: none executed (HELD — same reason).

## Pass 2 — lines 1100–3225 (full read @ 450733e5, seed out/bootstrap/bin/with-stage1)

### Findings
2. [CLOSED / no defect] src/ComptimeTransform.w:1156 — severity: none — target: T15 — `ct_transform_expr` NK_MATCH arm loop calls void `ct_transform_match_arm` (mutates arm in place via set_data1/set_data2, L510-516) then writes the unchanged `arm` id back with `extra.set_i32`. Harmless no-op writeback, not a dropped return (callee returns nothing). Refutation attempt: read callee body — no return value exists to drop. Probe status: covered by p6 Display-derive match run (PASS).
3. [CLOSED / no defect] src/ComptimeTransform.w:1638-1671 (SoA push) — severity: none — target: T13 — generated `push` rebinds `self`/`value` to `var` locals then passes `NK_MOVE_ARG(value.field)` per row; matches D32 ownership discipline in comments (bare `value.field` would be implicit field-move error). No use-after-move: `__soa_out` tail ident is a fresh node. Refutation attempt: p4 SoA run (new/push/len/get) PASS. Probe status: EXECUTED PASS.
4. [CLOSED / no defect] src/ComptimeTransform.w:1693-1698 (SoA get clone gate) — severity: none — target: T13/T15 — `.clone()` emitted when `field_tp_sym != 0 or is_copy(elem_tid) == 0`, covering #770 (type-param tid is 0 at transform time, `is_copy(0) == 1` would skip). Impl gains per-param Clone bound via `ct_copy_type_params_with_bound` (L1738). Refutation attempt: no counterexample; concrete path exercised by p4 PASS. Probe status: EXECUTED PASS (concrete fields).
5. [CLOSED / no defect] src/ComptimeTransform.w:2176/2237/2307/2448/2522 vs 2595 (derive receiver modes) — severity: none — target: T13/T22 — Eq/Hash/Ord/Debug/Display `self` params use FN_PARAM_FLAG_MOVE_SELF while Clone/Serialize use REF_SELF; each matches how the body uses the receiver (field moves into eq/cmp/hash/debug calls vs borrow). Error-decl Display/Debug use MOVE_SELF and Error::display uses REF_SELF with matching `deref_self` fstring flag (L2364). Refutation attempt: probes initially failed with "use of moved value" until written with move-aware bindings — confirming enforcement, not a generator bug. Probe status: EXECUTED PASS (p1/p2/p8).
6. [CLOSED / no defect] src/ComptimeTransform.w:2503-2519 (Display derive patterns) — severity: none — target: T15 — synthesized `NK_PAT_VARIANT(sym, extra, 1)` + `NK_PAT_REST` child and `NK_MATCH_ARM(pat, body, 0)` layouts match Parser.w constructions (cf. Parser.w:6103, 6338). Refutation attempt: compared field order directly; identical. Probe status: EXECUTED PASS (p6 prints `Red`, assert PASS).
7. [CLOSED / no defect] src/ComptimeTransform.w:1880-1904 (user-defined derive eval+parse) — severity: none — target: T13/T15 — `comptime_force_eval_expr_result` result string taken via documented `text.clone()` D32 workaround (L1884-1887); `Parser.init_with_pool(move tokens, source, ..., move self.diags, out)` hands `self.diags` out and restores it at L1898 — each moved field reassigned before reuse, no use-after-move. Same move-and-restore idiom at L3201-3202/3214/3223 (transform Sema handoff). Refutation attempt: traced every `move self.*` to its restore; all restore. Probe status: negative control n1 (`@[derive(Frobnicate)]`) correctly rejected with "unsupported derive target ... expected comptime function 'derive_frobnicate[T]() -> str'".
8. [CLOSED / no defect] src/ComptimeTransform.w:2711-2723 (Builder build fallback) — severity: none — target: T22 — missing-field fallback uses declared `default_node` clone when present else `return Err(MissingField)` via VARIANT_SHORTHAND, combined with `OP_DEFAULT`; `build`-named field pre-rejected (L2651-2656). Refutation attempt: negative control n2 (field named `build`) correctly rejected; p3 Builder run PASS. Probe status: EXECUTED PASS + 1 negative control PASS.
9. [CLOSED / no defect] src/ComptimeTransform.w:3005-3225 (`comptime_transform_module`) — severity: none — target: T15/T22 — ordered-decl rebuild preserves per-decl source path/file/c-import vectors; `local_decl_count` adjusted by `generated_local_count` only for local decls (L3060+); fresh transform Sema gets cloned module graph + tracked-input context, errors short-circuit before per-decl transform (L3213-3216). Field-sym source asymmetry (SoA reads syms from decl extras L1593; Default/Eq/Hash/Ord/Clone read from resolved `type_extra`) is benign — both denote the same fields; SoA additionally needs decl-extra type nodes. Refutation attempt: no caller mixes the two inconsistently; all derive probes PASS. Probe status: covered by p1-p8 runs.
10. [CLOSED / process] Finding 1 HELD item resolved — severity: none — target: T15/T22 — lines 1100–3225 now read in full (5 sequential window reads covering to L3225 EOF) plus targeted re-reads (L493-590 fstring/match-arm, L940-1010 comptime-for/fold, L300-340 ct_build_call). No defect alleged in pass 1 survives or extends; finding 1 is superseded by this verdict.

## Probes (pass 2, seed out/bootstrap/bin/with-stage1, /tmp/ctaudit2)
- p1 Clone+Debug run: PASS — prints `Point { x: 1, y: 2 }`, debug_str assert holds.
- p2 Default+Eq run: PASS — `Cfg.default() == Cfg.default()`, prints `ok`.
- p3 Builder run: PASS (needs `use std.result.BuilderError` import; without it compiler emits the §18.1 explicit-import error — correct behavior, not a defect).
- p4 SoA run: PASS — new/push(`t = t.push(...)` move-self rebind)/len/get asserts hold silently.
- p5 comptime-for run: PASS (`check` + `run`, sum 0..3 == 3).
- p6 Display derive run: PASS — prints `Red`, to_str assert holds.
- p8 Clone+Hash+Ord run: PASS — cmp nonzero assert, prints `ok`.
- Negative controls: n1 unknown derive target FAILS LOUDLY as required; n2 Builder field named `build` FAILS LOUDLY as required. Both PASS as controls.
- NOT executed: Serialize/Deserialize/ComponentId runtime round-trip, generic-parameter derive instantiation (probe p7 abandoned — generic struct-literal turbofish syntax not resolved; shared `ct_add_generated_impl_target` path covered by read-only review only). Early probe authoring errors (wrong `@(...)`/`type Name @[...]:` syntax, `Ok(v):` match arm, move-self receiver misuse) were probe bugs, fixed; each fix confirmed the compiler's documented ownership errors firing correctly.

## Recommendation
No defects to report; no upstream issues filed. Optional follow-up (not a finding): runtime round-trip probes for Serialize/Deserialize/ComponentId derives and a generic-derive instantiation probe.

## Pass 3 — independent re-verification (lines 1100–3225 @ 450733e5, seed out/bootstrap/bin/with-stage1, /tmp/ctaudit3)

### Findings
11. [CLOSED / no defect] src/ComptimeTransform.w:1155-1156 vs 510–516 — severity: none — target: T15 — `ct_transform_match_arm` is `mut fn ... (node: i32)` with no return type (mutates arm in place via set_data1/set_data2); L1156 writeback of the unchanged `arm` id is a harmless no-op, not a dropped value. Refutation attempt: read callee signature directly — no return value exists to drop. Probe status: covered by q1 Display run (PASS).
12. [CLOSED / no defect] src/ComptimeTransform.w:1652–1671, 1693–1698 — severity: none — target: T13 — SoA `push` passes `NK_MOVE_ARG(value.field)` per row with `var` rebinds (D32 discipline); `get` clone gate `field_tp_sym != 0 or is_copy(elem_tid) == 0` covers #770 (tp tid is 0 at transform time). Refutation attempt: q3 SoA run (new/push/len/get) PASS. Probe status: EXECUTED PASS.
13. [CLOSED / no defect] src/ComptimeTransform.w:2176/2237/2307/2448/2522 vs 2595/2832 — severity: none — target: T13/T22 — Eq/Hash/Ord/Debug/Display use FN_PARAM_FLAG_MOVE_SELF; Clone (L2595) and Serialize (L2832) use FN_PARAM_FLAG_REF_SELF. Refutation attempt: a first-draft q1 probe calling `c.to_str()` twice was rejected with "a moved value cannot be used again" — confirming MOVE_SELF enforcement is real, not a generator bug; single-use form passes. Probe status: EXECUTED PASS.
14. [CLOSED / no defect] src/ComptimeTransform.w:1884–1898 — severity: none — target: T13 — user-derive `text.clone()` D32 workaround plus `Parser.init_with_pool(move tokens, ..., move self.diags, out)` with restore at L1898; every `move self.*` in the handoff (incl. L3201–3202/3214/3223) is reassigned before reuse. Refutation attempt: negative control n3 (`@[derive(Frobnicate)]`) rejected with the exact L1869 "unsupported derive target ... expected comptime function" error. Probe status: EXECUTED PASS (control).
15. [CLOSED / no defect] src/ComptimeTransform.w:3005–3225 (`comptime_transform_module`) — severity: none — target: T15/T22 — ordered-decl rebuild preserves per-decl source path/file/c-import; `generated_local_count` added to `local_limit` only (L3192–3194); fresh transform Sema gets cloned module graph + tracked-input context; errors short-circuit before per-decl transform (L3213–3216). Refutation attempt: no caller mixes decl-extra vs resolved-type field-sym sources inconsistently; all derive probes PASS. Probe status: covered by q1–q3 runs.

## Probes (pass 3, seed out/bootstrap/bin/with-stage1, /tmp/ctaudit3)
- q1 Display derive `run`: PASS — prints `Red`, assert holds (`enum Color` + `@[derive(Display)]`, single-use `Color.Red.to_str()`; double-call draft correctly rejected as use-after-move).
- q2 Clone+Debug+Default+Eq `run`: PASS — prints `Point { x: 1, y: 2 }`, clone/eq asserts hold.
- q3 SoA `run`: PASS — new/push(`t = t.push(...)` move-self rebind)/len/get asserts hold silently.
- Negative control n3 unknown derive `check`: FAILS LOUDLY as required — "unsupported derive target 'Frobnicate'; expected comptime function 'derive_frobnicate[T]() -> str'". PASS as control.
- NOT executed: Serialize/Deserialize/ComponentId runtime round-trip, generic-parameter derive instantiation, Builder derive (covered by pass-2 p3 read-only review; not re-run in pass 3).
