# Primary verification — `lib/std/zlib/inflate.w`

Status: **COMPLETE** (no defects)
Primary verifier: primary (full source read + probe execution)
Source revision: `450733e5`
Source examined: all 5548 lines (three complete reads: 1–2000, 2001–4000, 4001–5548)

## Scope examined

Full inflate engine: `inflate` (`:17`, goto-state-machine over modes
16180–16211: header/gzip/zlib wrap handling, stored/fixed/dynamic block
decode, LEN/LIT/DIST/EXTRA/MATCH/COPY literal-match loop, CHECK/LENGTH/DONE
trailer verification), plus `inflateEnd` (`:4698`), `inflateSetDictionary`
(`:4719`), `inflateGetDictionary` (`:4769`), `inflateSync` (`:4800`),
`inflateCopy` (`:4898`), `inflateReset` (`:4976`), `inflateReset2`
(`:4995`), `inflatePrime` (`:5067`), `inflateMark` (`:5113`),
`inflateGetHeader` (`:5144`), `inflateInit_` (`:5165`),
`inflateInit2_` (`:5170`), `inflateSyncPoint` (`:5242`),
`inflateUndermine` (`:5262`), `inflateValidate` (`:5279`),
`inflateCodesUsed` (`:5305`), `inflateResetKeep` (`:5318`),
`inflateStateCheck` (`:5373`), `updatewindow` (`:5432`), `syncsearch`
(`:5502`). Deps: `std.zlib.defs` (state/types), `inftrees`
(`inflate_table` ×3 call sites `:2522`,`:3077`,`:3094`; `inflate_fixed`
`:1969`), `inffast` (`inflate_fast` `:3170`), `adler32`/`crc32` check
verification. Callers: `lib/std/zlib/uncompr.w:128,171`
(`uncompress2_z`), `lib/std/zlib/gzread.w:872` (`gz_decomp`),
`lib/std/zlib.w:144,159` (safe facade `zlib_inflate_to_buffer`),
`lib/std/zlib/example.w` tests, `lib/std/build.w` (via
`decompress_gzip_with_limit`).

## Behavioral matrix (EXECUTED vs HELD)

- `docs/audit/probes/zlib_inflate/main.w` — fixtures generated by CPython
  `zlib` (independent oracle, never self-derived): empty stream, level-0
  stored-blocks stream, `Z_FIXED` fixed-Huffman stream, level-9
  dynamic-Huffman stream — all byte-exact after `decompress()`; corrupted
  header (CMF `0x00`) and flipped-Adler streams both `is_err` (oracle:
  CPython raises `incorrect header check` / `incorrect data check` on the
  same bytes). ALL-PASS (7/7). EXECUTED.
- `test/behavior/behav_zlib_std.w` (repo's own suite, exercises this
  engine through the facade incl. gzip + levels + error paths) → `ok`.
  EXECUTED.
- `with check lib/std/zlib/inflate.w` → exit 0 (stage1). EXECUTED.
- `with check lib/std/zlib.w` (whole-package graph incl. this module) →
  exit 0. EXECUTED.
- Dictionary path (`inflateSetDictionary`, Z_NEED_DICT resume),
  `inflateSync` recovery, `inflatePrime/Mark/GetHeader/Copy`:
  no in-repo callers and not reachable through the safe facade. HELD
  (read-only review: control flow matches the canonical state machine;
  no anomaly spotted).

Caveat: `run` probes link the stage1-embedded stdlib copy; working-tree
sources verified by full read plus the checks above.

## Findings

None. In-report notes (not filed):
- Machine-migrated goto form (`__ci_bb_*`, `__local_*__goto_*`) is
  stylistically heavy but each examined transition (NEEDBITS pulls,
  NEEDOUT suspension at `'__ci_bb_28`, BAD deferred-error routing,
  window `updatewindow` accounting) preserves the canonical semantics;
  probes confirm end-to-end behavior on every block type.
- `inflateUndermine` (`:5262`) unconditionally sets `sane = 1` and
  returns `Z_DATA_ERROR`, matching upstream zlib semantics.

Verdict: COMPLETE
