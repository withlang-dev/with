With is a humane systems language: close to the machine, native by default, safe by design, and unwilling to make programmers write *anything* the compiler already knows.

Every unnecessary character is a compiler failure.

With pays compiler complexity to eliminate user ceremony. If the compiler can infer it, import it, prove it, generate it, link it, migrate it, or make it safe, the programmer should not have to spell it out.