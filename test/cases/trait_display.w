type Point = { x: i32, y: i32 }

impl Point =
    fn show(self: &Point) -> i32: self.x + self.y

fn main -> i32:
    let p = Point { x: 1, y: 2 }
    println(p.show())
    println(p.x)
    println(p.y)
