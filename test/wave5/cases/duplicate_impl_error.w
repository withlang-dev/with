trait Show =
    fn show(self: Self) -> i32

type Foo = {
    x: i32,
}

impl Show for Foo =
    fn show(self: Foo) -> i32:
        self.x

impl Show for Foo =
    fn show(self: Foo) -> i32:
        self.x

fn main -> i32:
    0
