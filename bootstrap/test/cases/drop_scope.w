type Guard = { name: str }

impl Guard =
    fn new(n: str) -> Guard: Guard { name: n }

fn main -> i32:
    let a = Guard.new("first")
    let b = Guard.new("second")
    println(a.name)
    println(b.name)
